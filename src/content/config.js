export const config = {
  // Screen 1: Secret Question
  secretQuestion: "What is the secret name which you like to call me",
  secretAnswer: "kanna", // Case-insensitive matching

  // Background configurations (Optional overrides if you don't want to replace the image files)
  // By default, the app will look for images in public/images/
  // screen2Background: "/images/screen2-bg.jpg",
};
